Files in this directory:

make_synthetic_data.m
- A matlab script that creates synthetic trajectory data

trajectory_data.txt
- The output of make_synthetic_data.m

bm_skel.m
- The script that runs the nested sampling algorithm

bm_results.txt
- The summary file from a run of bm_skel.m

ns_*
- Files that contains the program for running the nested sampling algorithm
